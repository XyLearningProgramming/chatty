# Chatty environment variables for Kubernetes secret
# Copy this file to chatty.env and fill in the actual values
#
# These are injected as env vars into the chatty pod via envFrom secretRef.
# The app reads them via pydantic-settings with CHATTY_ prefix and __ nested delimiter.

# LLM configuration
CHATTY_LLM__ENDPOINT=http://localhost:8000/api/v1/
CHATTY_LLM__API_KEY=your-api-key-here
CHATTY_LLM__MODEL_NAME=gpt-3.5-turbo

# PostgreSQL connection URI (asyncpg driver for async FastAPI usage)
# Passwords come from helmfile-secret-db in the db namespace (key: USER_PASSWORD)
# For local dev: postgresql+asyncpg://app:password@localhost:5432/app
CHATTY_THIRD_PARTY__POSTGRES_URI=postgresql+asyncpg://app:<USER_PASSWORD>@postgres.db.svc.cluster.local:5432/app

# Redis connection URI (redis-py asyncio compatible)
# Passwords come from helmfile-secret-db in the db namespace (key: REDIS_PASSWORD)
# For local dev: redis://:password@localhost:6379/0
CHATTY_THIRD_PARTY__REDIS_URI=redis://:<REDIS_PASSWORD>@redis-master.db.svc.cluster.local:6379/0
